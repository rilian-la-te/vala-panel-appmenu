if (ENABLE_XFCE)
    install(FILES ${CMAKE_CURRENT_BINARY_DIR}/appmenu.desktop DESTINATION share/xfce4/panel/plugins)
endif()
if (ENABLE_VALAPANEL)
    install(FILES ${CMAKE_CURRENT_BINARY_DIR}/appmenu.plugin DESTINATION lib/vala-panel/applets/)
endif()